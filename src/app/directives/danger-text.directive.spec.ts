import { DangerTextDirective } from './danger-text.directive';

describe('DangerTextDirective', () => {
  it('should create an instance', () => {
    const directive = new DangerTextDirective();
    expect(directive).toBeTruthy();
  });
});
